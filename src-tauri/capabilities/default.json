{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力（壳层 UI 为内核官方前端，无需 tauri IPC；spawn/kill 全在 Rust 侧）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
